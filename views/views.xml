<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="texture.list">
      <field name="name">Texture list</field>
      <field name="model">texture.texture</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="texture.action_window">
      <field name="name">Texture window</field>
      <field name="res_model">texture.texture</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="texture.action_server">
      <field name="name">Texture server</field>
      <field name="model_id" ref="model_texture_texture"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->

    <menuitem name="Texture" id="texture.menu_root"/>

    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="texture.menu_1" parent="texture.menu_root"/>
    <menuitem name="Menu 2" id="texture.menu_2" parent="texture.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="texture.menu_1_list" parent="texture.menu_1"
              action="texture.action_window"/>
    <menuitem name="Server to list" id="texture" parent="texture.menu_2"
              action="texture.action_server"/>
    -->
  </data>
</odoo>
